import { useEffect } from "react";

export const useCheckAuth = () => {
  useEffect(() => {
    // onAuthStateChanged(FirebaseAuth, async (user) => {
    //   if (!user) return;
    // });
  }, []);
};
